# AGENTS.md — Agent 工作说明

本文件为 agent 每次加载时的行为指南。请先阅读本文件，再执行任何任务。

## 目录用途

本目录是个人规划笔记库，用于存放个人目标、计划、复盘、灵感等规划类内容。所有内容均以 Markdown 格式撰写。

## 交流规范

- 与用户交流使用中文。
- 回复保持简洁、直接，避免冗长。
- 文档、笔记内容默认使用中文撰写；技术术语、文件名、代码可保留英文。

## 文件命名规范

- 文件名使用小写字母、数字、连字符（`-`），如 `2026-08-20-月度目标.md`。
- 日期前缀统一格式：`YYYY-MM-DD`。
- 同类内容归入同一文件，避免碎片化。

## 笔记结构规范

每篇笔记建议包含以下结构（按需取舍）：

```markdown
# 标题

## 背景 / 目标
## 计划 / 步骤
## 执行记录
## 复盘 / 收获
```

- 使用 `##` 作为二级标题，层级最多到四级（`####`）。
- 列表使用 `-`，不用 `*`。
- 日期与状态（如 `[ ]` 待办、`[x]` 完成）使用标准 Markdown 语法。
- 重要结论或行动项放在章节开头，细节放后面。

## 目录结构约定

- 根目录按内容主题分文件夹（如 `目标/`、`复盘/`、`灵感/`）。
- 临时内容可先放在根目录，主题清晰后移动到对应文件夹。
- 不要创建 `archive/` 等杂物目录，旧内容直接在原文件内标记 `~~已废弃~~` 或移入对应主题文件。

## 任务执行偏好

- 多步骤任务先创建任务清单，逐步执行并更新状态。
- 创建新文件前先确认目录结构，避免重复或杂乱。
- 修改已有文件前先阅读原文件，保留原有风格。
- 不确定需求时先询问，不擅自扩大范围。

## 本文件维护

- 根据与用户的对话持续更新本文件，使行为指南贴合实际需求。
- 更新时保持现有结构与风格，只补充或修正与用户明确达成的约定。
